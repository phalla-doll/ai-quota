{
    "name": "ai-quota-desktop",
    "version": "0.1.0",
    "private": true,
    "description": "Windows tray client for the Z.ai quota dashboard",
    "scripts": {
        "dev": "tauri dev",
        "build": "tauri build",
        "icons": "tauri icon ../app/icon.png"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2"
    }
}
